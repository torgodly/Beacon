# ─── Managed by Beacon — site: secure.example.com ───
server {
    listen 80; listen [::]:80;
    server_name secure.example.com;
    # Shared ACME webroot — works before the app is deployed, and for every site type.
location ^~ /.well-known/acme-challenge/ {
    root /var/www/beacon-acme;
    default_type "text/plain";
    allow all;
}
    location / { return 301 https://secure.example.com$request_uri; }
}
server {
     listen 443 ssl http2; listen [::]:443 ssl http2;
        server_name secure.example.com;
    ssl_certificate     /etc/letsencrypt/live/secure.example.com/fullchain.pem;
ssl_certificate_key /etc/letsencrypt/live/secure.example.com/privkey.pem;
ssl_trusted_certificate /etc/letsencrypt/live/secure.example.com/chain.pem;
add_header Strict-Transport-Security "max-age=31536000" always;

    root /home/beacon/secure.example.com/public;
    index index.php index.html;
    charset utf-8;

    add_header X-Frame-Options        "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff"    always;
    add_header Referrer-Policy        "strict-origin-when-cross-origin" always;

    client_max_body_size 100M;


    location / { try_files $uri $uri/ /index.php?$query_string; }
    location = /favicon.ico { access_log off; log_not_found off; }
    location = /robots.txt  { access_log off; log_not_found off; }
    error_page 404 /index.php;

    location ~ \.php$ {
        fastcgi_pass unix:/run/php/php8.3-fpm-secure-example-com.sock;
        fastcgi_split_path_info ^(.+\.php)(/.+)$;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        fastcgi_param DOCUMENT_ROOT   $realpath_root;
        fastcgi_hide_header X-Powered-By;
        fastcgi_read_timeout 60;
        fastcgi_buffers 16 16k;
        fastcgi_buffer_size 32k;
    }

    location ~ /\.(?!well-known).* { deny all; }

    access_log /var/log/nginx/secure.example.com-access.log;
    error_log  /var/log/nginx/secure.example.com-error.log error;
}
